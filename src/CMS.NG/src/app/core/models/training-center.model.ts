/** 教育中心 TrainingCenter — slim lookup row used as the tab strip on the 上稿作業 grid. */
export interface TrainingCenterLookup {
  /** 主代碼 */
  pkid: number;
  /** 名稱 */
  name: string;
  /** 顯示順序 */
  displayOrder: number;
}
