/**
 * Default (production) environment. Replaced by `environment.development.ts`
 * under the `development` build configuration — see `fileReplacements` in angular.json.
 */
export const environment = {
  production: true,
  apiBaseUrl: '/api',
};
